# PODF-TeamMakkaraperuna
Repository
saku kävi täällä
mike myös

Try to edit this file if you need a reminder how to use git.

git clone "URL"
git status
git add ./ git add -A
git commit -m "viesti tähän" 
git push

git pull

esc :wq

git branch
git branch "uusi oksa"
git checkout "oksa"
git merge master
git push --set-upstream origin "päivitysoksa"